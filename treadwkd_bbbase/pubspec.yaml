name: treadwkd_bbbase
description: "A new Flutter project."
version: 0.0.1
homepage:

environment:
  sdk: ^3.9.2
  flutter: '>=3.3.0'

dependencies:
  flutter:
    sdk: flutter
  plugin_platform_interface: ^2.0.2
  get: 4.6.6
  flutter_screenutil: 5.9.0
  event_bus: 2.0.0
  get_storage: 2.1.1
  flutter_staggered_grid_view: 0.7.0
  lottie: 2.6.0
  sqflite: 2.3.3+1
  webview_flutter: 4.13.0
  fluttertoast: 8.2.12
  outlined_text: 1.0.1
  flutter_android_ad_plugins: 0.0.8
  audioplayers: 6.1.0
  flutter_app_lifecycle: 0.0.3
  decimal: 3.2.4
  marqueer: 2.1.0
  spine_flutter: 4.2.36
  flutter_tba_info: 0.0.9
  flutter_check_adjust: 0.0.1
  firebase_analytics: 11.5.2
  firebase_core: 3.15.1
  firebase_remote_config: 5.4.7
  permission_handler: 12.0.1
  app_settings: 6.1.1
  connectivity_plus: 7.0.0
  flutter_custom_facebook: 0.0.5
  url_launcher: 6.3.2
  birdsong:
    path: plugins/common_push-output
  trea_feng:
    path: plugins/trea_feng

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

# The following section is specific to Flutter packages.
flutter:
  # This section identifies this Flutter project as a plugin project.
  # The 'pluginClass' specifies the class (in Java, Kotlin, Swift, Objective-C, etc.)
  # which should be registered in the plugin registry. This is required for
  # using method channels.
  # The Android 'package' specifies package in which the registered class is.
  # This is required for using method channels on Android.
  # The 'ffiPlugin' specifies that native code should be built and bundled.
  # This is required for using `dart:ffi`.
  # All these are used by the tooling to maintain consistency when
  # adding or updating assets for this project.
  plugin:
    platforms:
      android:
        package: com.treadwdw.treadwkd_bbbase
        pluginClass: TreadwkdBbbasePlugin
      ios:
        pluginClass: TreadwkdBbbasePlugin

  # To add assets to your plugin package, add an assets section, like this:
  # assets:
  #   - images/a_dot_burr.jpeg
  #   - images/a_dot_ham.jpeg
  #
  # For details regarding assets in packages, see
  # https://flutter.dev/to/asset-from-package
  #
  # An image asset can refer to one or more resolution-specific "variants", see
  # https://flutter.dev/to/resolution-aware-images

  # To add custom fonts to your plugin package, add a fonts section here,
  # in this "flutter" section. Each entry in this list should have a
  # "family" key with the font family name, and a "fonts" key with a
  # list giving the asset and other descriptors for the font. For
  # example:
  # fonts:
  #   - family: Schyler
  #     fonts:
  #       - asset: fonts/Schyler-Regular.ttf
  #       - asset: fonts/Schyler-Italic.ttf
  #         style: italic
  #   - family: Trajan Pro
  #     fonts:
  #       - asset: fonts/TrajanPro.ttf
  #       - asset: fonts/TrajanPro_Bold.ttf
  #         weight: 700
  #
  # For details regarding fonts in packages, see
  # https://flutter.dev/to/font-from-package
